# Copyright © 2025 Cask Data, Inc.
#  Licensed under the Apache License, Version 2.0 (the "License"); you may not
#  use this file except in compliance with the License. You may obtain a copy of
#  the License at
#  http://www.apache.org/licenses/LICENSE-2.0
#  Unless required by applicable law or agreed to in writing, software
#  distributed under the License is distributed on an "AS IS" BASIS, WITHOUT
#  WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied. See the
#  License for the specific language governing permissions and limitations under
#  the License.

name: Deploy Twill
on:
  schedule:
    - cron: '0 8 * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: cdapio-hub-k8-runner

    strategy:
      fail-fast: false
      matrix:
        branch: [ oss-exit-gate ]

    steps:
      - name: Get Secrets from GCP Secret Manager
        id: 'secrets'
        uses: 'google-github-actions/get-secretmanager-secrets@v0'
        with:
          secrets: |-
            CDAP_OSSRH_USERNAME:cdapio-github-builds/CDAP_OSSRH_USERNAME
            CDAP_OSSRH_PASSWORD:cdapio-github-builds/CDAP_OSSRH_PASSWORD
            CDAP_GPG_PASSPHRASE:cdapio-github-builds/CDAP_GPG_PASSPHRASE
            CDAP_GPG_PRIVATE_KEY:cdapio-github-builds/CDAP_GPG_PRIVATE_KEY

      - name: Recursively Checkout Repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          path: twill
          ref: ${{ matrix.branch }}

      - name: Cache
        uses: actions/cache@v3
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ github.workflow }}-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-${{ github.workflow }}

      - name: Set up GPG conf
        run: |
          echo "pinentry-mode loopback" >> ~/.gnupg/gpg.conf
          echo "allow-loopback-pinentry" >> ~/.gnupg/gpg-agent.conf

      - name: Import GPG key
        run: |
          echo "$GPG_PRIVATE_KEY" > private.key
          gpg --import --batch private.key
        env:
          GPG_PRIVATE_KEY: ${{ steps.secrets.outputs.CDAP_GPG_PRIVATE_KEY }}

      - name: Get Maven Project Version
        id: get_version
        working-directory: twill
        run: |
          MVN_VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)
          echo "version=$MVN_VERSION" >> "$GITHUB_OUTPUT"
          echo "Detected Maven version: $MVN_VERSION"

      - name: Submit Build to GCB
        id: gcb
        working-directory: twill
        run: |
          echo "Submitting build for version ${{ steps.get_version.outputs.version }} (Deploy Type: ${{ steps.get_version.outputs.deploy_type }}) to Cloud Build..."
          gcloud builds submit . \
            --config=cloudbuild.yaml \
            --project='cdapio-github-builds' \
            --substitutions="_DEPLOY_TYPE=${{ steps.get_version.outputs.deploy_type }},_GPG_PASSPHRAS=${{ steps.secrets.outputs.CDAP_GPG_PASSPHRASE }},_SONATYPE_USERNAME=${{ steps.secrets.outputs.CDAP_OSSRH_USERNAME }},_SONATYPE_PASSWORD=${{ steps.secrets.outputs.CDAP_OSSRH_PASSWORD }}"
